<p style="color: green"><%= notice %></p>

<h1>Nice News</h1>

    <div id="nuevas">
      <div class="container text-center">
        <div class="row">
          <% @nuevas.each do |nueva| %>
            <div class="card bg-light col-2">
              <%= render nueva %>
              <p>
                <%= link_to "Mostrar esta noticia", nueva %>
              </p>
            </div>
          <% end %>
        </div>
      </div>
    </div>

<%= link_to "Nueva Noticia", new_nueva_path %>

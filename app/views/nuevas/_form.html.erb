<%= form_with(model: nueva) do |form| %>
  <% if nueva.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(nueva.errors.count, "error") %> prohibited this nueva from being saved:</h2>

      <ul>
        <% nueva.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :encabezado, style: "display: block" %>
    <%= form.text_field :encabezado %>
  </div>

  <div>
    <%= form.label :cuerpo, style: "display: block" %>
    <%= form.text_field :cuerpo %>
  </div>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.number_field :tipo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
